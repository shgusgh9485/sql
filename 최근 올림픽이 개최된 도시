SELECT year, SUBSTR(UPPER(city), 1, 3) AS city  //UPPER(컬럼명) -> 대문자로 변환
FROM games                                      //SUBSTR(~~, 시작위치, 추출개수) -> (~~, 1, 3)이면 1번째부터 3개 반환(1, 2, 3번째)
WHERE year >=2000
ORDER BY year DESC;
